<%  @pending_audit_items.each do |pending_audit_item|%>
<%= link_to "I confirm that I did not have Overtime for the week of: #{pending_audit_item.start_date} - #{pending_audit_item.start_date + 7}" , confirm_audit_log_path(pending_audit_item),  class: "btn btn-primary btn-block btn-lg", id: "confirm_#{pending_audit_item.id}",data:  { confirm: 'Are you sure you want to confirm that you did not perform any overtime for the stated week?'} %>
<% end  %>
